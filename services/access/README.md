Access-control preparation layer for future paid plans.

Current role:
- provide plan metadata for `free` and `premium`
- expose feature flags for premium-only capabilities
- expose usage policy metadata such as daily analysis limits

Important:
- no restriction is enforced yet
- current MVP remains fully usable for guest/local mode
- later enforcement can be added in API/service flows without changing UI code
